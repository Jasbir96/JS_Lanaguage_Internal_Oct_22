// == -> first check the type -> if types are different -> try convert it into same type
// and then compare it's values`
// === -> first it check's the type -> if it's different -> it will give false

console.log(1 == "1") // true
console.log(1 === "1") // false
